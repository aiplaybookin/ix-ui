# ix-ui

# Install 
pip install -r requirements.txt

# Watch GPU
```
watch -n 1 nvidia-smi
```

1. Fix validation dataset
Use the below code and fix the dataloader for training and validation. There should be a split of data from the file 
2. During validation : why it is not utilizing the GPU?
